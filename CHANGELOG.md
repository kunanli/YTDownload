# 更新紀錄

版本號遵循 [語意化版本](https://semver.org/lang/zh-TW/)。

## 1.23.0

### 修正

- **yt-dlp 會把使用者的 cookies 檔案改寫掉，於是「第一次成功，之後永遠失敗」。**

  只要收到 `cookiefile`，yt-dlp 在結束時就會把整個 cookie jar 寫回那個檔案
  （`YoutubeDL.save_cookies`）。YouTube 每次工作階段都會輪替 session cookie，所以
  跑一次就把使用者辛苦匯出來的檔案覆蓋成「只對那一次有效」的版本。實測：一個只有
  一行的 cookies.txt，跑一次 yt-dlp 之後變成六行。

  症狀完全不會讓人想到原因——使用者只看到第一次成功、第二次開始又是
  `Sign in to confirm you're not a bot`，於是以為是自己匯錯，重匯、再壞、再重匯。
  最清楚的一次是 `doctor`：三個探針裡前兩個 ✔，第三個就已經 ✖ 了，cookies 在同一次
  指令裡就被寫壞。

  現在每次交給 yt-dlp 的都是一份用完即丟的副本，原檔永遠不動。而且每次都從原檔重新
  複製——同一次執行裡的每個 YoutubeDL 都拿到乾淨的起點，不會互相汙染。

### 說明

匯出的 cookies 還是會自然過期（通常幾週），那時重匯一次即可。但至少不會再「用一次
就壞掉」——這兩件事先前混在一起，完全分不出來。

## 1.22.2

### 修正

- **`doctor` 把本機檔案錯誤的路徑截掉了，而那條路徑就是全部的線索。** 失敗訊息一律
  切在 110 字，對站台的錯誤剛好（後面都是重複的說明），但 `[Errno 13] Permission
  denied: 'C:\…\Firefox\Profiles\xxxx.default…'` 這種訊息**最重要的檔名就在被切掉的
  那一段**——看不出卡住的是 `cookies.sqlite`、是設定檔目錄，還是別的東西。

  這實際害人多繞了一整輪：Firefox 都關乾淨了還是同一個錯誤，卻無從判斷下一步。
  現在 cookies 類的錯誤完整保留，其餘照舊截短。

## 1.22.1

### 修正

- **「讀不到瀏覽器的 cookies」被講成「這條網路不通」。** 瀏覽器還開著的時候，
  cookies 資料庫是鎖住的，yt-dlp 拿到的是 `[Errno 13] Permission denied`——這是本機的
  問題，連線一個封包都還沒送出去，但 `doctor` 的結論會叫使用者去換網路、查防毒。

  這是繼 1.21.1 之後同一類的誤判：三種連線方式全滅，不代表原因就是連線。現在
  「讀不到 cookies」排在最前面判斷，直接說出最常見的原因——**那個瀏覽器還在跑**
  （關掉視窗還會留背景程序，得到工作管理員結束它）。

- 同一段說明也接到下載失敗的結尾。讀不到 cookies 會讓每一首都失敗，看起來跟被擋
  一模一樣，但正解是關掉瀏覽器，不是等退燒、換 IP 或加 `--slow`。
- `is_blocked_error()` 不再把 cookies 錯誤算成「被擋」——先前會讓整批在第 10 首停手，
  理由還是個假的。

## 1.22.0

### 修正

- **「連續三首被擋就停掉整批」會讓使用者一首都拿不到。** 這是 1.20.0 引進的規則，
  前提是「這不是影片的問題，是整批都過不去」——但那個前提常常不成立。

  `Sign in to confirm you're not a bot` 這句話有兩種完全不同的成因：整個 IP 被擋是一種，
  **某些影片本來就要登入才看得到**是另一種，而 YouTube 對兩者回的是同一句話。實測
  （同一秒、同一個 IP、沒有任何下載歷史的機器）：`dQw4w9WgXcQ`、`w4XAz8Nlk4c` 讀得到，
  `FIgyep0mJa8`、`79fzeNUqQbQ` 回這句話。所以「有的行、有的不行」是常態。

  一張 50 首的清單開頭剛好混到三支這種影片，舊規則就會把整批停掉——另外三十幾首
  明明下得動。現在門檻提高到 10，而且**只要有一首成功過就永遠不停手**：有東西下得動，
  就證明不是整批被擋。

- **被擋時的建議改成先教怎麼分辨**，而不是直接給一串招數。分不出是哪一種的話，
  拿一支平常下得動的影片單獨試一次就知道——這一句比前面那四點加起來都有用。
  兩份 README 也照同樣的結構重寫了。

  講錯方向的代價很實際：以為是 IP 被擋，就會去等、去換網路、去調併行數、去裝 deno，
  而真正的答案從頭到尾只有 cookies 一個。

## 1.21.1

### 修正

- **`doctor` 把「被站台擋下來」誤判成「這條網路不通」。** 三種連線方式全滅時，它一律
  說「這條網路到這個站台是不通的」——但被 YouTube 認出 IP 的時候長得一模一樣，
  而這兩件事的處置完全相反：一個是停手等它退燒，一個是去查防毒、VPN、換網路。
  判斷錯的代價是使用者花一整晚查一個根本沒壞的東西。

  現在會先看失敗的內容：是封鎖就直說「它認得這個 IP，跟網路通不通無關」，並提醒
  每多試一次都會讓標記更久。

### 說明

被擋之後想確認「還被擋著嗎」，用 `doctor "網址"` 而不是再跑一次下載——doctor 只
向站台問一次，真正的下載一首要問七、八次。反覆用下載去試，等於一邊等退燒一邊加柴。

## 1.21.0

### 修正

- **「一首一首下載得動、整批就被擋」現在有對症的那一招了。** 1.20.0 會在被擋時停手
  並給建議，但建議裡最有用的那條（放慢）根本做不到——工具只能調同時下載數，沒有
  任何辦法在兩次請求之間留間隔。使用者手動一個一個貼網址會成功，正是因為打字天然
  就隔了十幾秒；YouTube 看的是**密集程度**，不是總共下了幾首。

  新增 `--slow`（等同 `-j 1 --sleep 2`）與 `--sleep 秒`。下載歷史會自動略過已完成的，
  所以被擋之後直接重跑就是接著跑。被擋時的建議也改成把 `--slow` 排在第一，cookies
  退到後面——cookies 最容易弄錯，而且弄錯之後的症狀是另一種錯誤訊息。

  間隔會乘得很兇（解析一首要問站台七、八次）。實測同一支影片、單首從解析到存檔：
  不節流 8 秒、`--sleep 2` 35 秒、`--sleep 5` 78 秒。所以 `--slow` 定在 2 秒——
  43 首約 25 分鐘；5 秒要一小時，太重了。

- **選單使用者終於有得選了。** `下載.bat` 沒有地方可以打 `-j` 或 `--sleep`，而他們正是
  最常撞到這面牆的人。整批下載時現在會直接問「慢慢下載？」，選 `y` 就是加了 `--slow`。
  也可以 `config set request_sleep 2` 設成永久預設。

### 新增

- 設定檔新增 `request_sleep`，讓選單與排程也吃得到節流

### 測試

新增一個結構性檢查：把程式碼裡每一個 `t("…")` 用到的鍵，都拿去跟訊息表對一遍。

起因是這次改版真的踩到了——一次大段替換把相鄰的四塊訊息一起吃掉，而既有的 i18n
測試完全沒反應：它驗的是「表裡的每個鍵都有六種語言」，鍵整個消失反而驗不到。
`t()` 找不到鍵時回傳鍵名本身，所以畫面上只會多印一行 `cookies.hint`，不會拋例外。
是 `test_cli.py` 的建議文字斷言先變紅才抓到的。

## 1.20.0

### 修正

- **`unable to download video data: HTTP Error 403: Forbidden` 的真正原因，終於說得出來了。**
  YouTube 會把播放網址裡的一段參數用 JavaScript 打亂，yt-dlp 得真的跑得動那段 JS
  才解得開。機器上沒有 JS runtime 時它解不開，拿到的網址被回 403——而訊息從頭到尾
  不會提到缺了什麼，看起來就只是「這支影片不能下載」。

  現在 `doctor` 會直接驗這一項（列在 ffmpeg 旁邊，缺了是 ✖ 不是 !，因為 YouTube
  現在真的要它），下載失敗時也會把它排在 cookies 建議**前面**——那是自己這台機器
  就能修好的，把最好修的原因藏在後面沒有道理。

- **機器上有 Node.js／Bun 卻不拿來用**。yt-dlp 預設只自動啟用 `deno`，其餘的要
  明講。現在偵測到什麼就替使用者講出來，不必自己去讀 yt-dlp 的旗標。

- **被當成機器人時，不再讓剩下的幾百首陪葬。** 連續三首撞到
  `Sign in to confirm you're not a bot` 或 403 就停手——這種失敗跟影片無關，是這個
  IP／這次工作階段整個被擋下來了，照原樣跑完只是讓封鎖更久，而且會把真正的原因
  沖到畫面外（496 首的清單會刷出 490 行一模一樣的錯誤）。

  沒輪到的曲目改回報成「取消」而不是「失敗」，結尾只留一個數字：
  `完成 0 首，失敗 1 首，411 首沒有下載`。

- **Windows 上的 `--cookies-from-browser chrome` 幾乎一定失敗**，但先前的提示還是
  先建議 chrome。Chrome 127 起 cookies 改用應用程式綁定加密，yt-dlp 解不開，只會說
  `cookies could not be decrypted`。現在建議一律以 Firefox 為主，並在 Windows 上
  額外說明為什麼 Chrome 不行。

- 「被擋下來」和「這支影片要帳號才看得到」分成兩段不同的建議。兩者都靠 cookies
  解決，但該做的事不同——混成同一句話，使用者只會照著錯的那半邊試。

### 新增

- **可以下載 YouTube 自己推薦的混音清單（Mix，`list=RD…`）了。** 它沒有盡頭：
  實測完整展開一張跑了四分半還沒結束，因為 YouTube 會一直生下一首。現在預設停在
  前 50 首，並在畫面上講明停在哪、怎麼改。
- `--max N`：每個網址最多下載幾首，`--max 0` 表示不設限。一般清單也適用。
  上限會在展開階段就交給 yt-dlp（`playlistend`），不是事後才截斷——混音清單是一頁
  一頁問出來的，問越多次也越容易被盯上。
- 選單遇到混音清單會直接問「要幾首」，按 Enter 就用預設值。

### 說明

混音清單是**依帳號推薦**的。沒帶 cookies 時，YouTube 會給一份以那支影片為種子、
但不認得你的版本——想要畫面上看到的那一份，還是得帶 cookies。

## 1.19.0

### 修正

- **啟動檔被 Windows 的假 `python` 騙過去**。`where python` 會找到
  `%LOCALAPPDATA%\Microsoft\WindowsApps\python.exe`——那是 App Execution Alias，
  名字在 PATH 裡、執行卻只印
  `Python was not found; run without arguments to install from the Microsoft Store`。

  於是檢查「通過」了，下一步 `pip install -e .` 才莫名其妙失敗，畫面上只留一句
  「Install failed」。使用者看到的是設定莫名其妙壞掉，而不是「你沒有 Python」。

  現在**真的把每個候選跑一次**（`py -3` → `py` → `python` → `python3`，再加上
  `%LOCALAPPDATA%\Programs\Python\Python3*`），並要求版本 ≥ 3.9。

### 新增

- **沒有 Python 時的引導安裝**，兩步：
  1. 說明 Store 佔位程式是怎麼回事，然後問要不要用 winget 幫你裝
  2. 明確告訴使用者「關掉視窗、再雙擊一次」——新裝的 Python 進不了已開視窗的 PATH
- 沒有 winget 就給手動步驟，並特別提醒安裝程式第一頁最常被漏勾的
  `Add python.exe to PATH`，以及怎麼關掉 App Execution Alias
- 安裝失敗時建議 `ensurepip`，並提到公司網路的 proxy／SSL 可能是原因
- macOS 的 `下載.command` 同樣改成「真的跑跑看」，並用 Homebrew 引導

### 測試

啟動檔完全在 Python 測試範圍之外——壞了不會有測試變紅，只有使用者會看到。
新增 `tests/test_launchers.py` 把踩過的坑釘住：純 ASCII、換行格式、
必須真的執行直譯器而不是只查名字、版本下限、兩步引導、以及
`.gitattributes` 的 eol 設定。

## 1.18.0

### 新增

- **「解釋」也跟著語言走**。1.17.0 只翻了選單，但真正需要看懂的是失敗時那幾行：
  - 下載流程：解析中、共幾首、輸出設定、完成／失敗統計
  - 失敗訊息：`✖ 無法讀取 …`、連線問題的五步建議、重試時說明改用了哪一招
  - `doctor`：環境檢查、連線測試、每一項相依的狀態、四種結論
  - 微信視頻號：查詢、線上解析、拿不到時的完整說明
  - 短網址展開：同意流程與「以後都自動展開嗎」
- 沒設定過語言時**照系統語系自動選**（`LANG=ko_KR.UTF-8` → 韓文），
  認不出來就用英文；設定檔裡選過的永遠優先
- 語言在 `main()` 最前面就套用，所以 `dl`、`doctor`、`wechat` 看到的訊息一致

### 說明

不翻的只有兩處，都是刻意的：`--help` 的參數說明（打指令的人本來就在讀英文旗標），
以及 `下載.bat` 自己那三句（`cmd.exe` 會在 Python 啟動前把非 ASCII 弄壞）。

`doctor` 的結論改成用訊息代號比對而不是比對中文字串——否則換了語言之後，
它會認不出自己剛剛跑過哪一招。

## 1.17.0

### 新增

- **介面語言切換**：中文（繁體）· 日本語 · English · 한국어 · Español · Suomi
  - 選單新增 `[L]`，選完立刻寫進設定檔，下次打開就是那個語言
  - 第一次執行選單時問一次，之後不再問（非互動環境不問，避免卡住排程）
  - `config set ui_language ja` 也可以直接設
  - `[L]` 這一列在每種語言下都帶著 "Language" 這個字——就算現在的介面完全看不懂，
    也找得到出口
- **英文版 README**（`README.en.md`），兩份互相連結

### 說明

翻譯範圍是「雙擊啟動之後看到的所有文字」：選單、提示、缺相依的詢問。下載過程的
進度與錯誤訊息目前仍是中文，用同一套機制可以再補。

`下載.bat` 自己那三句訊息刻意維持英文——`cmd.exe` 會在 Python 啟動前就把非 ASCII
文字弄壞，這也正是整個選單當初就做在 Python 裡的原因。

字幕／歌詞語言的**值**（`繁中`、`英`…）不隨介面語言改變：那是 `lyrics.py` 用來
比對的別名，改了會壞。只有畫面上顯示的名稱會翻譯。

### 測試

翻譯表本身有測試把關：每個訊息代號都必須六種語言齊全、不能有多餘語言、
各語言的 `{placeholder}` 必須一致——少一句就紅。

## 1.16.0

### 新增

- 短網址展開**同意一次就記住**：答應展開之後會再問一次「以後都自動展開嗎？」，
  答 `Y` 就寫進設定檔，之後不再詢問
- 設定項 `expand_short_urls`（預設關閉，因為會把網址送給第三方）

### 文件

- **補上四個先前漏掉的章節**。快速查表裡有四列指向不存在的錨點——
  1.12～1.15 的說明文字因為標題層級寫錯（`###` vs `##`）而靜默沒有寫入：
  - 「先跑 doctor 讓它告訴你問題在哪」
  - 「短網址被擋住」（含 `--expand` 流程與 curl_cffi 的已知問題）
  - 「SSL 連線被切斷」與「只有 LinkedIn 這樣？那多半是 TLS 指紋」
  加了一段檢查，把 README 裡所有內部連結對照實際標題驗一次
- 「雙擊啟動選單」補上**啟動流程**：找 Python → 沒裝套件就自動
  `pip install -e .` → 選單再檢查 yt-dlp / mutagen / ffmpeg / curl_cffi
- 下載選項表補上 `--impersonate`、`--expand`、`--no-expand`、`--expander-url`
- 預設設定補上 `expand_short_urls` 與 `impersonate`

### 已確認

使用者實測：`lnkd.in` 短網址展開成完整的 `linkedin.com` 網址後**下載成功**
（1080p，49 MiB/s）。先前的假設得到證實——被擋的是短網址那個網域本身。

## 1.15.0

### 新增

- **短網址展開**：連線被切斷且網址是 `lnkd.in`、`bit.ly` 這類轉址服務時，
  換成它指向的完整網址再試一次
  - `--expand` 不再詢問直接展開，`--no-expand` 永遠不用，`--expander-url` 可自架
  - 跟微信的線上解析一樣：**送出網址前一定先問過**，且只送網址、不送 cookies
- `doctor` 把短網址與完整網址**分開測**，結論會直接說是哪一種被擋
- **缺相依時主動提醒並幫忙裝**：
  - 選單一開啟就檢查 yt-dlp / mutagen / ffmpeg / curl_cffi，缺了就說明少掉什麼功能
    並問要不要幫裝（必要的才主動問，選用的只列出來）
  - ffmpeg 走 `winget`／`brew`；Linux 只給手動步驟（不替使用者提權）
  - `下載.bat` 與 `下載.command` 在套件本身沒裝時自動 `pip install -e .`
  - 非互動環境一律不裝，只印手動步驟

### 說明：LinkedIn 為什麼一直失敗

使用者裝上 `curl_cffi` 之後，假扮瀏覽器那招終於跑到了，但換成另一個錯：

```
curl: (35) TLS connect error: error:00000000:invalid library (0):OPENSSL_internal
```

這是 curl_cffi 在 Windows 上的已知問題（[yt-dlp#15385]、[curl_cffi#601]），
兩個 issue 都還開著、沒有修法——那條路暫時是死的。

回頭看才發現關鍵：**每一次失敗的都是 `lnkd.in` 短網址**，完整的 `linkedin.com`
網址從頭到尾沒被測過。`lnkd.in` 是轉址網域，出現在大量追蹤器封鎖清單裡；
這類封鎖看網域名稱就把連線切掉，症狀正是 `UNEXPECTED_EOF`。

所以與其想辦法「連上 lnkd.in」，不如繞過它。

[yt-dlp#15385]: https://github.com/yt-dlp/yt-dlp/issues/15385
[curl_cffi#601]: https://github.com/lexiforest/curl_cffi/issues/601

## 1.14.0

### 新增

- `ytmusic doctor [網址]`（選單 `[9]`）：一行指令問完該問的
  - 列出 Python、yt-dlp、ffmpeg、mutagen、curl_cffi、playwright 的狀態
  - **curl_cffi 分得出「沒裝」與「裝了但版本不對」**——這兩種 yt-dlp 都只說
    「target 不可用」，使用者只會反覆執行同一行沒用的指令
  - 給網址時，用一般連線／強制 IPv4／假扮瀏覽器**各試一次同一個網址**，
    再給一句「所以你該做什麼」

### 為什麼做這個

使用者第三次回報 LinkedIn「還是不行」。但他截圖裡印出了安裝提示——那行只在
`impersonation_available()` 為假時才會出現，也就是 **curl_cffi 在他機器上還不能用**，
所以假扮瀏覽器那招根本沒跑到。

靠截圖來回猜「裝了沒、版本對不對、是網址還是網路的問題」既慢又常猜錯。
把該問的都做成一行指令之後，答案自己會跳出來。

## 1.13.0

### 新增

- **假扮瀏覽器的 TLS 指紋**（yt-dlp 的 impersonation，需要 `curl_cffi`）：
  - 連線被切斷且 IPv4 也救不回來時**自動再試一次**
  - `--impersonate [瀏覽器]` 可從一開始就用，也能 `config set impersonate chrome` 固定
  - 沒裝 `curl_cffi` 時，失敗訊息會附上安裝方式
- 選用相依 `pip install "ytmusic[impersonate]"`

### 說明

使用者換了一個 LinkedIn 短網址，IPv4 重試也失敗，錯誤仍是
`[SSL: UNEXPECTED_EOF_WHILE_READING]`。但他的 YouTube 與微信視頻號都正常，
所以 Python 的 TLS 本身沒問題——是 **LinkedIn 那邊**把連線切斷。

這正是 TLS 指紋阻擋的典型症狀：對方依握手特徵判斷來的是不是瀏覽器，
不像就直接斷線。開發環境看不到這個現象，因為連外會經過一層會終止 TLS 的代理，
對方看到的是代理的指紋而不是 Python 的。

**注意 `curl_cffi` 的版本區間**：yt-dlp 只接受 `0.10 ≤ 版本 < 0.16`，
裝到 0.16 只會得到「Impersonate target 不可用」而完全不提版本——
安裝提示因此一律帶上範圍。

### 未驗證

開發環境無法重現使用者的斷線（代理擋在中間），也無法在這裡實測
impersonation 是否解決他的問題。已驗的是：重試順序、可用性偵測、
選項傳遞、以及裝上 `curl_cffi 0.15` 後 yt-dlp 確實列得出 impersonate 目標。

## 1.12.0

### 新增

- 連線在半路被切斷時**自動改用 IPv4 再解析一次**（等同 yt-dlp 的 `-4`）。
  IPv6 路由半通不通、或中間設備只擋 IPv6 時，症狀就是連線被無故切斷
- 判斷得出「這是連線問題」而不是「這支影片不能下載」，並給對應的建議：
  重跑、短網址換完整網址、關防毒的 HTTPS 掃描、關 VPN、換網路

### 說明

1.10.0 修好錯誤訊息之後，使用者的 LinkedIn 失敗終於顯示出真正的原因：

```
[SSL: UNEXPECTED_EOF_WHILE_READING] EOF occurred in violation of protocol
```

同一個網址在開發環境連得上（TLS 握手 0.5 秒、301 轉址正常），所以不是網址、
也不是 yt-dlp 版本，而是那條網路上有東西把 TLS 連線切斷了——防毒軟體的
HTTPS 掃描是最常見的成因。原本這種失敗跟「影片不存在」印一樣的訊息，
使用者只會往錯的方向找。

## 1.11.0

### 更正：微信視頻號**可以**下載

1.10.0 寫「`/sph/` 短連結不可能抓到」是**錯的**。使用者指出
[ltaoo/wx_channels_download](https://github.com/ltaoo/wx_channels_download)
的線上解析服務，實測直接推翻了那個結論：

| 查了什麼 | 結果 |
| --- | --- |
| 同一支 `get_feed_info` API，從這裡問 | `entryScene 64`，只有標題／作者／封面 |
| 同一支 API，從線上服務問 | `entryScene 51`，**有 `h264VideoInfo.videoUrl`** |
| 下載那個位址 | 3.0 MB `ftypisom` MP4，44.5 秒，**沒有加密**，直接可播 |

`entryScene` 是伺服器自己決定的，改請求內容改不動——同樣的請求換個出口 IP 問，
微信給不給影片就不一樣。所以那不是「做不到」，是「這個網路環境問不到」。

錯在把「我這裡問不到」寫成「不可能」。已改掉所有這類斷言。

### 新增

- 微信視頻號**直接下載**：拿到位址就下載，不開瀏覽器、不需登入、不需解密
- `--resolver`：微信不給位址時改請線上服務代查。**一定會先問過**才送出網址，
  並說明送的是什麼、送去哪裡；`--no-resolver` 永遠不問也不送，
  `--resolver-url` 可指向自架的服務。非互動環境一律不送
- 檔名從整段貼文說明收成標題長度（先砍結尾 hashtag，再從標點截斷）——
  原本會產生 120 字、塞滿 `#UE #虚幻引擎 #黑科技` 的檔名

### 變更

- 選單 `[5]` 不再問「要不要免開視窗」「要不要先登入」，那些是瀏覽器模式的問題
- 瀏覽器模式改成只在 `--browser` 時才走

## 1.10.0

### 修正

- **錯誤訊息被終端機吃掉**。yt-dlp 在支援顏色的終端機（Windows Terminal 就是）
  會把錯誤寫成 `\x1b[0;31mERROR:\x1b[0m …`，而清理訊息時是用 `;` 切句子——
  整句因此被切成 `\x1b[0`，印出去變成一段未完成的控制序列，把後面的字一起吃掉。
  這就是使用者先看到「無法讀取 <網址>：」一片空白、1.9.1 之後變成
  「…：DownloadError]」的原因。現在一律先清掉顏色碼與控制字元

  同一個 LinkedIn 短網址在開發環境用 yt-dlp 2026.07.04 實測**下載成功**，
  所以真正的失敗原因會由這次修好的訊息說出來（多半是 yt-dlp 版本較舊）。

### 新增

- 微信視頻號**先直接問 API，不必開瀏覽器**：一秒就知道這支影片拿不拿得到，
  拿得到就直接下載，拿不到也立刻說明——不會再等滿 120 秒才失敗
- `wechat --browser`：即使微信已表明網頁端沒有影片，仍然開瀏覽器試一次

### 說明：`/sph/` 短連結為什麼不可能抓到

查證結果（不是推測）：

| 查了什麼 | 結果 |
| --- | --- |
| 分享頁 HTML | 2,595 bytes 的 JS 外殼，沒有影片位址 |
| `get_feed_info` API | 只回標題、作者、封面，**沒有 `videoUrl`** |
| 頁面播放器程式碼 | `pageMode === "sph"` 時播放被關掉，點擊只跳 QR code |
| 改用 `exportId` 問 | 回「此内容暂时无法播放」 |
| 頁面實際發出的 14 個請求 | 最大的影片 CDN 資源是 46.9 KB 的封面 jpg |

影片位址從來沒被送到網頁端，所以瀏覽器攔不到——不是登入的問題，也不是等不夠久。
舊訊息寫「可能是還沒登入、影片沒開始播放」是在誤導使用者，已經改掉。

## 1.9.2

### 修正

- 選單收到空輸入時直接踢回選單，貼上一旦落空就得從頭再選一次。改成**重問**
  （最多三次），並在重問時說明怎麼貼上、以及輸入 `q` 可放棄

使用者回報「貼上功能失效」——選了項目、看到提示，畫面卻立刻跳回選單。
不論根因是終端機沒貼進去還是貼到的內容夾帶換行，一收到空值就放棄都是錯的：
連重貼的機會都沒有。

## 1.9.1

### 修正

- 錯誤訊息短到無從判斷時補上例外型別。使用者回報 LinkedIn 失敗，畫面只顯示
  「無法讀取 <網址>：」後面一片空白，完全查不出原因
- 讀取網址失敗時提示可加 `-v` 看完整診斷、或先更新 yt-dlp

### 說明

同一個 LinkedIn 短網址（`lnkd.in/p/...`）在開發環境實測**下載成功**
（3.5 MiB mp4，且不需要 cookies），所以問題出在使用者端的環境而非網址本身；
最可能是 yt-dlp 版本較舊。

## 1.9.0

### 改名

工具早已不只支援 YouTube，介面卻還寫著「YouTube 音樂下載器」：

- 選單橫幅改成「影音下載器」，並列出 YouTube · Bilibili · Vimeo · 微信視頻號
- 選單項目改成「下載音樂／影片（任何網站，貼網址）」
- `--help` 的說明與範例改寫，涵蓋各站台與新指令
- 套件說明同步更新

套件名稱與指令維持 `ytmusic` 不變——改名會破壞既有安裝、指令習慣與說明文件，
不值得。

### 文件

- 新增「LinkedIn 的影片網址怎麼複製」：右鍵的 Copy video address 被
  LinkedIn 鎖成灰色，要改用貼文右上角 `•••` → Copy link to post，
  或點時間戳開啟貼文專頁再複製網址列。Facebook 與 Instagram 同理

## 1.8.0

### 修正

- **瀏覽器來不及掃碼就關閉**：「抓到影片了」的判斷寫成「有沒有來自影片 CDN 的
  回應」，但封面圖也放在 finder.video.qq.com 上，一載入就命中，迴圈立刻跳出、
  視窗隨即關掉。改成必須是「非縮圖且大小夠」才算抓到

### 新增

- `wechat --login`：只開瀏覽器掃碼登入，**視窗不會自動關閉**，按 Enter 才收。
  登入與下載分開，掃碼就沒有時間壓力
- 直接讀 `get_feed_info` API 的回應找影片位址，比猜「頁面下載了什麼」可靠，
  影片還沒開始播放時也拿得到
- 選單選 [5] 後直接按 Enter 就是登入模式

## 1.7.0

### 修正

- **抓到封面當影片**：`vweixinthumb.tc.qq.com` 的 thumb 就是縮圖，卻被誤列進
  「影片主機」清單，導致挑選器把 47 KB 的封面當成影片下載。現在縮圖主機與
  `image/*` 內容一律排除，不會被選中
- 加上大小門檻：小於 300 KB 的回應幾乎不可能是視頻號影片，排序時降權
  （伺服器沒給 content-length 時不當作否定證據）
- Range 請求會把影片切成多段，各段的 content-length 只是片段大小；
  改成優先讀 `content-range` 的總長度，才問得出真正的檔案大小

### 新增

- 抓不到影片或抓到的檔案不能播時，列出**頁面實際發出的所有請求**
  （大小、類型、網址，並標示 CDN／縮圖／m3u8），否則只能瞎猜

## 1.6.0

### 新增

- 微信視頻號第一次執行時，缺 Playwright 或 Chromium 會**詢問後自動安裝**，
  不必事先準備任何東西；`-y` 可跳過詢問
- 安裝一律走 `python -m pip` 與 `python -m playwright`

### 修正

- 安裝提示原本寫 `playwright install chromium`，但 pip 裝的執行檔在 Windows
  上不在 PATH，使用者會得到「不是內部或外部命令」——跟先前 `ytmusic` 短指令
  同一個坑。提示改成 `python -m playwright install chromium`，並加測試確保
  提示不會再出現裸的 `playwright` 或 `pip install`

### 說明

安裝前一律先問過。非互動環境（stdin 不是終端機）一律不自動安裝，改為印出
手動步驟——擅自在使用者機器上裝東西不該是預設行為。

## 1.5.0

### 新增

- 啟動選單的搜尋可以選站台：CLI 早就有 `--site bilibili`，但選單沒開放，
  等於雙擊使用者只能搜 YouTube
- 微信網址貼進「下載音樂」或「下載影片」時自動轉去走瀏覽器模式，
  不再直接失敗——使用者不該需要知道哪個選單編號對應哪個平台

### 設計說明

貼網址的選項刻意**不問**平台：yt-dlp 看網址就能決定用哪個 extractor，
多問一步只是增加摩擦。只有搜尋需要問，因為關鍵字看不出想搜哪個站台。

## 1.4.0

### 新增

- `ytmusic wechat <網址>`：微信視頻號下載，走瀏覽器自動化而非流量攔截
  - 開一個真正的瀏覽器載入頁面，攔截**頁面自己發出的**請求取得影片位址，
    因此不必安裝根憑證、也不必代理整台電腦的流量
  - 登入狀態存在設定目錄，掃碼一次之後可用 `--headless` 免視窗執行
  - 抓到的檔案若不是已知的影片容器（可能拿到加密串流）會明確告知並刪除，
    不會留下打不開的檔案
  - Playwright 為選用相依：`pip install "ytmusic[wechat]"`
  - 支援 `YTMUSIC_CHROMIUM` 指定瀏覽器路徑、自動沿用系統代理設定
  - 啟動選單新增 `[5] 下載微信視頻號`，會問要不要免視窗執行
    （`下載.bat` 本身不需更動——它只呼叫 `ytmusic menu`）

### 已驗證與未驗證

- 已驗證：瀏覽器啟動、頁面載入、請求攔截、來源挑選、下載、容器格式檢查
  （用本機伺服器完整跑過一次，攔到 6.4 MB 的 video/mp4 並正確下載）
- 未驗證：微信掃碼登入與視頻號頁面本身——開發環境沒有微信帳號，
  且容器的瀏覽器無法連外

## 1.3.0

### 新增

- Facebook：公開影片與 Reels 直接貼網址即可（實測下載成功）
- Instagram：貼文、Reels、限動可下載，但幾乎都要登入，需搭配
  `--cookies-from-browser`

README 的站台章節改成對照表，逐一標明各站台是「直接可用」還是「需登入」，
並在快速查表補上 Instagram 未登入時的錯誤訊息。

## 1.2.0

### 新增

- Vimeo 與 LinkedIn：網址直接貼即可（yt-dlp 本來就有 extractor，
  1.1.0 修掉寫死 YouTube 的假設之後才真的能用）
- Vimeo 的一般頁面要先換 OAuth token，部分網路環境會被回 401；
  遇到時自動改用 player.vimeo.com 網址重試，未公開影片的雜湊會保留

### 說明

- 微信視頻號補上第二款工具 wx_video_download（把下載鈕注入微信播放頁面，
  比另一款順手），並補上完整的憑證與代理還原步驟。兩款的原理相同：
  本機代理攔截流量，需要開著微信手動播放，因此一樣無法整合

## 1.1.0

### 新增

- Bilibili 支援：影片網址直接下載，合集、收藏夾、UP 主投稿列表都會展開
- `search --site bilibili`：在 Bilibili 搜尋

### 修正

- 非 YouTube 站台的封面會抓到死連結：原本任何影片 ID 都會被拼成
  `i.ytimg.com` 網址，現在只有 YouTube 才這樣推導
- Bilibili 的 BV 號有 12 碼，被 YouTube 的「超過 11 碼＝頻道」規則誤刪，
  該規則現在只套用在 YouTube
- 影片沒有縮圖時，站台可能回一張 1×1 的透明佔位圖（Bilibili 的
  `transparent.png`），原本會被當成專輯封面寫進檔案；現在會驗證尺寸後才寫入

### 說明

- 微信視頻號無法用網址下載：分享頁是純 JS 外殼，影片位址需要微信客戶端帶著
  簽章與 session token 才換得到，串流本身也是加密的。README 改為指向
  qiye45/wechatvideodownload——它靠 mitmproxy 攔截流量並解密，需要開著微信
  手動播放，無法自動化，因此不整合進本工具

## 1.0.0

第一個正式版。功能齊全、301 個測試、在 Windows 與 Linux 上都實機跑過。

### 下載

- 音訊下載與轉檔：mp3 / m4a / opus / flac / wav，96–320 kbps 或 `best`
- 影片下載：`--video`，可指定 360p–2160p 畫質上限，輸出相容性最好的 H.264 + AAC MP4
- 播放清單與頻道批次下載，可設定並行數與清單子資料夾
- 網址同時含單曲與清單時會詢問，避免誤抓數百首的自動混音

### 找歌

- `search`：用歌名搜尋並挑選，支援 `3`、`1,3,5`、`2-4`、`a` 全選等輸入
- `search -a`：用歌手名稱找歌，只留該歌手自己頻道的單曲
- 官方音源標 `♪`、超過 15 分鐘的合輯標 `≡`

### 中繼資料

- 自動寫入歌名、演出者、專輯、年份與正方形專輯封面
- 從標題移除 `(Official Music Video)`、`【官方MV】` 等宣傳字樣後再命名檔案
- 歌詞 `--lyrics`：字幕轉成帶時間軸的 `.lrc`，同時寫進 ID3 標籤
- 字幕 `--subs`：影片內嵌獨立字幕軌，可自行開關
- 語言支援繁中、簡中、英、日、韓、西班牙，可用中文別名指定

### 管理

- SQLite 下載歷史，重跑清單自動略過已下載的曲目
- `sync`：訂閱播放清單，一行指令補上新增的歌
- `config`：常用設定存成預設值
- `menu`：互動式選單，雙擊 `下載.bat` / `下載.command` 即可使用

### 其他

- 需要登入的影片可用 `--cookies-from-browser`，支援指定瀏覽器設定檔
- `-v` 印出完整診斷訊息
- 進度顯示以顯示欄寬計算，中日韓歌名不會破版
